import 'package:flutter/material.dart';

const kDefaultPadding = 20.0;
const kPrimaryColor = Color(0xFFF48120);
const kBackgroundColor = Color(0xFFFFFFFF);

// IP padrão de LocalHost quando utilizamos emulador
// Caso for rodar em um dispositivo físico, alterar esse IP
const kApiUrlMobile = "http://10.0.2.2:3000";
const kApiUrlWeb = "http://localhost:3000";
